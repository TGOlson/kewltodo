<% @lists.each do |list| %>
	<p><%= link_to list.name, list_path(list) %>
	<%= link_to 'complete', list_path(id: list.id, list: { complete: !list.complete }), method: :put %>
	<%= link_to 'delete', list_path(list), method: :delete %></p>
<% end %>